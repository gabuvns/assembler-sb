#ifndef ASSEMBLER_H
#define ASSEMBLER_H
using std::cout;
using std::endl;
using std::ifstream;
using std::string;
using std::vector;

void analyzeCode(ifstream &inFile, string outputFileName, int argNum);

#endif